---
id: inbox_2427f799
date: 2026-06-29
source_ref: "[[00-inbox/.../inbox_2427f799]]"
title: "v13.9.0"
url: https://github.com/thedotmack/claude-mem/releases/tag/v13.9.0
source: claude-mem-releases
published_at: 2026-06-29T22:28:52+00:00
fetched_at: 2026-07-02T01:14:54.800524+00:00
model: claude-haiku-4-5
tokens_in: 0
tokens_out: 0
summary_zh: "claude-mem v13.9.0 推出重大新功能 `claude-mem/sdk`（cmem-sdk），提供全記憶體內 capture → compress → semantic-search pipeline，無需 HTTP worker 和 Redis。開發者可直接 import `createCmemClient` 並指向 Postgres + `uvx chroma-mcp` + 任意 LLM 提供商，逐次呼叫 capture/generate/search/context/session 方法。SDK 依賴（pg、zod、@modelcontextprotocol/sdk、@anthropic-ai/sdk）保留為 external，由消費端解析。運行時從 `server-beta` 重命名為 `server`，移除冗餘的 ProviderRegistry/EventBroadcaster 邊界，簡化隊列解析。Bug 修復：generate() 提供商異常不再卡在 processing，直接轉換為 failed 狀態並記錄 last_error；search() 空查詢改正確回報 chroma:false（濾選模式，非降級）。"
key_points:
  - "新 SDK 架構：記憶體內管道無需 HTTP worker/Redis，依賴 Postgres + Chroma MCP，支援 capture/compress/semantic-search"
  - "運行時重命名 server-beta → server，簡化內部邊界設計，保留後向兼容性別名"
  - "生成失敗處理改善：異常狀態從 processing → failed，捕獲 last_error；搜尋空查詢正確標記 chroma:false"
tags: [claude-mem, sdk, in-process, chroma-mcp]
topics: [foundation_models.claude, agents.mcp]
importance: 4
novelty: 4
insight_quality: 4
insight_type: tool
deep_dive_candidate: false
deep_dive_approved: false
---

## v13.9.0

claude-mem v13.9.0 推出重大新功能 `claude-mem/sdk`（cmem-sdk），提供全記憶體內 capture → compress → semantic-search pipeline，無需 HTTP worker 和 Redis。開發者可直接 import `createCmemClient` 並指向 Postgres + `uvx chroma-mcp` + 任意 LLM 提供商，逐次呼叫 capture/generate/search/context/session 方法。SDK 依賴（pg、zod、@modelcontextprotocol/sdk、@anthropic-ai/sdk）保留為 external，由消費端解析。運行時從 `server-beta` 重命名為 `server`，移除冗餘的 ProviderRegistry/EventBroadcaster 邊界，簡化隊列解析。Bug 修復：generate() 提供商異常不再卡在 processing，直接轉換為 failed 狀態並記錄 last_error；search() 空查詢改正確回報 chroma:false（濾選模式，非降級）。

### 重點
- 新 SDK 架構：記憶體內管道無需 HTTP worker/Redis，依賴 Postgres + Chroma MCP，支援 capture/compress/semantic-search
- 運行時重命名 server-beta → server，簡化內部邊界設計，保留後向兼容性別名
- 生成失敗處理改善：異常狀態從 processing → failed，捕獲 last_error；搜尋空查詢正確標記 chroma:false

**原文：** [claude-mem-releases](https://github.com/thedotmack/claude-mem/releases/tag/v13.9.0)

---

### 📄 原文內容

<details>
<summary>點此展開 / 收合</summary>

# v13.9.0

Highlights 
 🚀 New: `claude-mem/sdk` (cmem-sdk) 
 A fully in-process capture → compress → semantic-search pipeline with no HTTP worker and no Redis . Import `createCmemClient` from `claude-mem/sdk`, point it at Postgres + a running `uvx chroma-mcp` + an LLM provider, and call `capture`/`generate`/`search`/`context`/session methods directly. 
 
 New reference docs: CMEM-SDK Reference under SDK &amp; Embedding . 
 Bundle keeps `pg`, `zod`, `@modelcontextprotocol/sdk`, and `@anthropic-ai/sdk` external so consumers resolve them against the installed package. 
 
 ♻️ Server runtime rename 
 `server-beta` → `server` across the runtime, with intentional back-compat aliases for existing settings files. Removed inert `ProviderRegistry`/`EventBroadcaster` boundaries and consolidated the queue resolver. 
 🐛 Fixes 
 
 `generate()`: a provider crash or parse error no longer leaves a job stuck in `processing`; it is transitioned to terminal `failed` with `last_error` recorded before re-throwing. 
 `search()`: empty-query path now reports `chroma: false` (filter-only, not degraded) instead of falsely claiming a Chroma result. 
 CI: the docker e2e job now calls the renamed `e2e:server:docker` script. 
 Docs: corrected `sdk.mdx`'s stale parse-error behavior note. 
 
 Full PR: #3077

</details>